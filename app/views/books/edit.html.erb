<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>
  <div>
    <p>Title</p>
  </div>
  <%= f.text_field :title %>

  <div>
    <p>Opinion</p>
  </div>
  <%= f.text_area :body %>

  <div>
    <%= f.submit "Update Book" %>
  </div>

  <%= link_to "Show", book_path(@book.id) %>
  <%= link_to "Back", books_path %>
<% end %>